# secrets
.env

# python
sidecar/.venv/
__pycache__/
*.pyc
sidecar/build/
sidecar/dist/
*.spec

# node
frontend/node_modules/
# NOTE: frontend/dist IS committed so the root double-click launcher can serve the UI
# with only Python (no Node build step). Rebuild with: npm --prefix frontend run build

# rust / tauri
src-tauri/target/
src-tauri/gen/
# frozen sidecar binaries are build artifacts
src-tauri/binaries/

# models / weights (downloaded on the target machine)
sidecar/app/weights/
*.pt
*.pth
*.safetensors

# built standalone app dropped in the project root (platform-specific build artifact)
/Neuclip Studio
/Neuclip Studio.exe
/Neuclip Studio.app/

# os
.DS_Store
Thumbs.db
