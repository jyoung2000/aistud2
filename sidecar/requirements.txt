# Neuclip Studio sidecar deps.
#
# CUDA PyTorch is NOT pinned here because the correct wheel depends on the installed
# CUDA toolkit. On the RTX 4070 target install it explicitly, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# then verify:  python -c "import torch; print(torch.cuda.is_available(), torch.cuda.get_device_name(0))"
#
# Phase 0 only needs the web stack; ML libs are added in later phases.

# --- web / sidecar ---
fastapi>=0.115,<0.117
uvicorn[standard]>=0.30,<0.35
pydantic>=2.7,<3
httpx>=0.27,<0.29
python-dotenv>=1.0,<2
python-multipart>=0.0.9        # /load file upload (Phase 3)
pillow>=10.3
numpy>=1.26,<3

# --- CPU ML / imaging (run everywhere; GPU paths layer on top) ---
opencv-python-headless>=4.9    # fallback select (GrabCut/flood), matting, live-wire, composite
scipy>=1.11                    # gradients / zero-crossings (Phase 4)
pyyaml>=6                      # .nprofile parsing (Phase 8)
jsonschema>=4                  # .nprofile validation (Phase 8)

# --- heavy / GPU (install separately on the target; auto-detected at runtime) ---
# torch, torchvision           # install per-CUDA: --index-url https://download.pytorch.org/whl/cu124
# sam2                         # SAM 2 (Phase 3) — from facebookresearch/sam2 + a checkpoint
# transformers                 # BiRefNet edge matting (Phase 3)
